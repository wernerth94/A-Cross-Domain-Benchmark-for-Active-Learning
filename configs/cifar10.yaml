data:
  batch_size: 512
  n_classes: 10

encoder:
  encoder_dim: 512
  feature_dim: 128

optimizer:
  type: SGD
  lr: 0.4
  nesterov: False
  weight_decay: 0.0001
  momentum: 0.9
  lr_scheduler: cosine
  lr_scheduler_decay: 0.1

clr_loss:
  temperature: 0.1

training:
  epochs: 500
